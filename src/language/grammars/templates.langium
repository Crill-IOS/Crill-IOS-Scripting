import 'common'

// Template commands – werden durch die Completion durch vollständige
// Konfigurationsblöcke ersetzt (insert-Text in Command_Details.json).

Template_ospf_cmd:
    '/ospf' CR
    ;

Template_rip_cmd:
    '/rip' CR
    ;

Template_grundkonfig_cmd:
    '/grundkonfig' CR
    ;
